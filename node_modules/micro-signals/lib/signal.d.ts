import { ExtendedSignal } from './extended-signal';
import { ReadableSignal } from './index';
import { Listener, WritableSignal } from './interfaces';
export declare class Signal<T> extends ExtendedSignal<T> implements WritableSignal<T>, ReadableSignal<T> {
    static setDefaultListener(listener: Listener<any>): void;
    private static _staticDefaultListener;
    protected _listeners: Set<Listener<T>>;
    constructor();
    dispatch(payload: T): void;
    setDefaultListener(listener: Listener<T>): void;
    private _instanceDefaultListener;
}
