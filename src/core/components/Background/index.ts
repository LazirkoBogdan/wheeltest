import Controller from "./BackgroundController";
import View from "./BackgroundView";
import Model from "./BackgroundModel";

export const BackgroundComponent = new Controller(new Model(), new View());
