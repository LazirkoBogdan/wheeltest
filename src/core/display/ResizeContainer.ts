import { Container } from "pixi.js";
import { enableResize } from "../../handlers/ResizeHandler";
export default class ResizeContainer extends Container {
  constructor() {
    super();
   //   enableResize(this)
  }
}
